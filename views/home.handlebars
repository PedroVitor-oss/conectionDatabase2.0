
{{{navigate}}}
{{{header}}}


<script>
    let clients = [
        {{#each dataClients}}
           "cliente"
        {{/each}}
    ]
</script>
<script>
{{#each styles}}
    $("head").append(`<link rel="stylesheet" href="{{css}}">`)
{{/each}}

{{#each scripts}}
    $("body").append(`<script src="{{js}}">`)
{{/each}}
</script>